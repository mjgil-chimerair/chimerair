[package]
name = "chimera-rust-ownership"
version = "0.1.0"
edition = "2021"
description = "Rust ownership and drop facts for Chimera"
license = "Apache-2.0"

[dependencies]
chimera-rust-schema = { path = "../chimera-rust-schema" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
