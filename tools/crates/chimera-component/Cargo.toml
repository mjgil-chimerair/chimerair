[package]
name = "chimera-component"
description = "Chimera component identity: ComponentId, ComponentKind, Language, ComponentSpec, TargetSpec, ProfileSpec, ModuleMap, ImportMap"
version = "0.1.0"
edition = "2021"
license = "ISC"
rust-version = "1.85"

[features]
default = ["std"]
std = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"